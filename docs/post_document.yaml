tags:
- Document
summary: Searching documents
produces:
- application/json
parameters:
- in: body
  name: body
  description: Finds a document containing the specified text
  required: false
  schema:
    type: object
    properties:
      text:
        type: string
responses:
  "200":
    description: successful operation
